{
  "name": "@tertium/vscode-js-shared-configs",
  "version": "0.2.0",
  "description": "Visual Studio Code JS shared configs.",
  "exports": {
    "biome": "./biome.json"
  },
  "scripts": {
    "release:patch": "npm version patch && npm run release:git",
    "release:minor": "npm version minor && npm run release:git",
    "release:major": "npm version major && npm run release:git",
    "release:git": "git checkout main && git merge develop && git push origin main && git tag v$(node -p \"require('./package.json').version\") && git push origin --tags",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vitalii Balabanov",
  "license": "ISC"
}
